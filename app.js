const add = (num1, num2) => {
    return num1 + num2;
}

const numStr = (num) => {
    return num.toString()
}

const planets = ["sun","mercury","venus","earth","mars","jupiter", "saturn","uranus","neptune"]

module.exports = {
    add,
    numStr,
    planets,
}

